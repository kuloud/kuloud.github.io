name: homepage
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.2 <4.0.0'
  flutter: 3.22.3

dependencies:
  flutter:
    sdk: flutter
  flutter_adaptive_scaffold: ^0.1.11+1
  flutter_markdown: ^0.7.3
  flutter_svg: ^2.0.10+1
  x_common: ^1.0.4
  http: ^1.2.2
  # x_common:
  #   path: ../flutter_dashboard/packages/x_common/
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/